apiVersion: v1
kind: Service
metadata:
  name: node-demo-app
  labels:
    app.kubernetes.io/name: node-demo-app
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: node-demo-app
