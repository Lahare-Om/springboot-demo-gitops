apiVersion: v1
kind: Service
metadata:
  name: python-demo-app
  labels:
    app.kubernetes.io/name: python-demo-app
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: python-demo-app