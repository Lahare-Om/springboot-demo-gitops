apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: springboot-demo

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: python-demo-app
    newTag: 0.1.0