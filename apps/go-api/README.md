# Go API

A simple Go REST API for testing the pipeline.

## Build
```bash
go build -o go-api main.go
```

## Run
```bash
./go-api
```

## Endpoints
- `GET /` - Returns a greeting message
- `GET /health` - Health check endpoint
