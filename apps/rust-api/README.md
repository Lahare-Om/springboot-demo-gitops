# Rust API

A simple Rust REST API using Actix-web for testing the pipeline.

## Build
```bash
cargo build --release
```

## Run
```bash
cargo run
```

## Endpoints
- `GET /` - Returns a greeting message
- `GET /health` - Health check endpoint
