# .NET API

A simple ASP.NET Core REST API for testing the pipeline.

## Build
```bash
dotnet build
```

## Run
```bash
dotnet run
```

## Endpoints
- `GET /` - Returns a greeting message
- `GET /health` - Health check endpoint
