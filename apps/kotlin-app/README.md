# Kotlin API

A simple Spring Boot API written in Kotlin for testing the pipeline.

## Build
```bash
mvn clean package
```

## Run
```bash
mvn spring-boot:run
```

## Endpoints
- `GET /` - Returns a greeting message
- `GET /health` - Health check endpoint
